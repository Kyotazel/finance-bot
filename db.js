// No longer needed: const mysql = require("mysql2/promise");
// No longer needed: require("dotenv").config();
// No longer needed: const pool = mysql.createPool({ ... });
// No longer needed: module.exports = pool;
